[workspace]
resolver = "2"
members = [
    "treasure-hunt",
    "soulbound-token",
    "player-registry",
]

[workspace.package]
version = "1.0.0"
edition = "2021"
rust-version = "1.75"

[workspace.dependencies]
soroban-sdk = "21.0.0"
soroban-token-sdk = "21.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
